name: "CI"

on: ["push", "pull_request"]

jobs:
  main:
    strategy:
      fail-fast: false
      matrix:
        os:
          - "ubuntu-latest"
          - "macos-latest"
          - "windows-latest"
        node_version:
          - 16
          - 18

    name: "Node ${{ matrix.node_version }} on ${{ matrix.os }}"
    runs-on: "${{ matrix.os }}"

    steps:
      - name: "Checkout repository"
        uses: "actions/checkout@v6"

      - name: "Set up Node ${{ matrix.node_version }}"
        uses: "actions/setup-node@v6"
        with:
          node-version: ${{ matrix.node_version }}
          cache: "yarn"

      - name: "Install dependencies"
        run: "yarn install --immutable"

      - name: "Test building package"
        run: "yarn run build"

      - name: "Run linting"
        run: "yarn run lint"

      - name: "Run tests"
        run: "yarn run test --full-trace"

  docs:
    strategy:
      fail-fast: false

    name: "Doc Link Checker"
    runs-on: "ubuntu-latest"

    steps:
      - name: "Checkout repository"
        uses: "actions/checkout@v6"

      - name: "Run Doc Link Checker"
        uses: "docker://ghcr.io/djmattyg007/doc-link-checker:1.0.4@sha256:ab3530c2e65ffc2f563c53b5e8f8a313f95dd67c11623a2fddb5af5dfcf87006"
        with:
          args: "lint --exclude-extend tests/fixtures/**"
        env:
          DOC_LINK_CHECKER_MDTYPE: "gfm"
